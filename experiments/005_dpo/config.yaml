experiment_id: 005_dpo
title: "DPO Alignment"
status: planned   # planned | running | completed

model:
  base: checkpoints/qwen3-8b-base
  adapter_out: adapters/005_dpo

dataset:
  source: datasets/instruction/
  max_samples: null

training:
  config: configs/training/sft_qlora.yaml

evaluation:
  benchmarks:
    - HumanEval
    - MBPP
