experiment_id: 004_bug_fix
title: "Bug Fixing SFT"
status: planned   # planned | running | completed

model:
  base: checkpoints/qwen3-8b-base
  adapter_out: adapters/004_bug_fix

dataset:
  source: datasets/instruction/
  max_samples: null

training:
  config: configs/training/sft_qlora.yaml

evaluation:
  benchmarks:
    - HumanEval
    - MBPP
