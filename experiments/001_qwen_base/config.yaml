experiment_id: 001_qwen_base
title: "Qwen3-8B Base Model Verification"
status: planned   # planned | running | completed

model:
  base: checkpoints/qwen3-8b-base
  adapter_out: adapters/001_qwen_base

dataset:
  source: datasets/instruction/
  max_samples: null

training:
  config: configs/training/sft_qlora.yaml

evaluation:
  benchmarks:
    - HumanEval
    - MBPP
