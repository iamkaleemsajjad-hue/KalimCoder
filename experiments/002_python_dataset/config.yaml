experiment_id: 002_python_dataset
title: "Python Dataset SFT"
status: planned   # planned | running | completed

model:
  base: checkpoints/qwen3-8b-base
  adapter_out: adapters/002_python_dataset

dataset:
  source: datasets/instruction/
  max_samples: null

training:
  config: configs/training/sft_qlora.yaml

evaluation:
  benchmarks:
    - HumanEval
    - MBPP
